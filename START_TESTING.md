# Начало тестирования непроверённых векторов

**Дата:** 2026-08-10  
**Версия:** 1.0  

---

## 🚀 Быстрый старт

### Зависимости
```bash
pip install pycryptodome numpy
```

### Запуск мастер-меню
```bash
cd C:\Users\Gigabyte\Desktop\THB
python RUN_TESTS.py
```

Выберите режим:
- **[0]** — Меню (выбор конкретного теста)
- **[1]** — Все тесты по порядку
- **[2]** — Быстрый режим (1,2,4 пропуск 3)
- **[3]** — Выход

---

## 📋 Тесты в порядке приоритета пользователя

### 1️⃣ DROP PROBABILITY (F-12) — MEDIUM, 1-2ч

**Файл:** `test_drop_probability.py`

Что ищет:
- Таблицу весов дропа в памяти (float значения 0.7, 0.25, 0.05)
- Возможность изменить вероятность редких предметов

Протокол:
1. Запустить скрипт
2. Открыть Cheat Engine
3. Сканировать память: `Scan for Float > 0.7`
4. Уточнить: `0.25`, затем `0.05`
5. Получить адрес таблицы
6. Вставить в скрипт
7. Изменить веса (редкие → 100%)
8. Открыть 10-20 сундуков → подсчитать редкие
9. Проверить после автосейва

Результат:
- ✅ УЯЗВИМО: > 50% редких предметов
- ❌ ЗАЩИТА: обычное распределение

---

### 2️⃣ CUBE SYNTHESIS (F-10) — HIGH, 2-3ч

**Файл:** `test_cube_synthesis.py`

Что ищет:
- Структуру рецепта синтеза куба в памяти
- Поле стоимости (обычно int32)
- Возможность установить отрицательную цену

Протокол:
1. Запустить скрипт
2. Открыть Cheat Engine
3. Найти известный ID рецепта (Scan for int32)
4. Определить поле стоимости (соседнее число)
5. Вставить адрес в скрипт
6. Изменить цену на отрицательное значение
7. В игре попытаться синтезировать
8. Если синтез прошёл → дождаться автосейва
9. Проверить, добавились ли ресурсы

Результат:
- ✅ УЯЗВИМО: ресурсы добавились вместо траты
- ❌ ЗАЩИТА: ошибка "недостаточно материалов"

---

### 3️⃣ RARE BOX ROLLS (F-11) — HIGH, 2-3ч

**Файл:** `test_rare_boxes.py`

Что ищет:
- BoxData структуру в памяти (из F-07 известна раскладка)
- Поле типа сундука (910651=обычный, 920651=редкий)
- Поле награды (RewardItemId)

Протокол:
1. Запустить скрипт
2. Открыть Cheat Engine
3. Использовать box_queue_prediction.json как справку
4. Найти ID сундука (Scan for int32)
5. Определить смещения полей (TYPE, REWARD)
6. Вставить адреса в скрипт
7. Изменить тип сундука: 910651 → 920651
8. Открыть сундук в игре
9. Проверить: выпала ли редкая награда?
10. Дождаться автосейва и перезагрузить

Результат:
- ✅ УЯЗВИМО: редкий предмет выпал и сохранился
- ❌ ЗАЩИТА: выпала оригинальная обычная награда

---

### 4️⃣ NETWORK HIJACKING (F-09) — CRITICAL, 3-4ч

**Файл:** `test_network_hijacking.py`

Что ищет:
- Возможность расшифровать сейв (пароль из F-06 известен)
- Модифицировать данные
- Переш cifровать и отправить на сервер

Протокол:
1. Запустить скрипт
2. Указать путь к сейву (рабочий файл .sav)
3. Ввести пароль ES3 (из F-06)
4. Скрипт расшифрует сейв
5. Выбрать значение для поиска (напр. 1207 = maxCompletedStage)
6. Выбрать вхождение и новое значение
7. Скрипт переш cifрует с изменением
8. Заменить оригинальный файл сейва на .hijacked
9. Запустить игру, загрузить сейв
10. Проверить: применилось ли изменение?
11. Дождаться автосейва (3 мин) и перезагрузить
12. Расшифровать новый сейв и проверить

Результат:
- ✅ УЯЗВИМО: изменённое значение пережило рестарт
- ❌ ЗАЩИТА: сервер откатил на оригинальное значение

---

## 🔑 Важные файлы

| Файл | Назначение |
|------|-----------|
| `RUN_TESTS.py` | Главное меню и запуск |
| `test_*.py` | Тестовые скрипты (4 штуки) |
| `box_queue_prediction.json` | Справка по сундукам из F-07 |
| `equip_chain.md` | Адреса и смещения в памяти |
| `FINDINGS.md` | Куда писать результаты (F-09...F-12) |

---

## 🛠️ Требуемые инструменты

- **Cheat Engine** — для поиска структур в памяти
- **Python 3.8+** — для запуска скриптов
- **pycryptodome** — для криптографии (Network Hijacking)
- **x64dbg** или **WinDbg** — для отладки (опционально)

---

## ✅ Чек-лист перед началом

- [ ] Python 3 установлен
- [ ] `pip install pycryptodome numpy` выполнен
- [ ] Cheat Engine установлен и работает
- [ ] TaskBarHero запущена и готова к тестам
- [ ] Есть рабочий сейв для тестирования
- [ ] Пароль ES3 известен (из F-06)

---

## 📝 Заполнение результатов

После каждого теста заполнить в `FINDINGS.md`:

```markdown
### F-XX — [Название находки]

| Метрика | Значение |
|---------|----------|
| Тест | PASSED / FAILED |
| Severity | CRITICAL / HIGH / MEDIUM / INFO |
| Статус | УЯЗВИМО / ЗАЩИЩЕНО |
| Примечание | ... |
```

---

## ⏱️ Примерная таблица времени

```
День 1:
  F-12 (Drop):      08:00 - 09:30 (1.5ч)
  F-10 (Cube):      09:30 - 12:00 (2.5ч)
  
Перерыв
  
День 2:
  F-11 (Rare):      08:00 - 11:00 (3ч)
  F-09 (Network):   11:00 - 15:00 (4ч, с проверкой сохранения)

Итого: ~11 часов чистого тестирования
```

---

## 🆘 Если что-то не работает

### Не найдено значение в памяти
- Попробовать другое значение (соседнее число)
- Проверить, что значение не защищено (Obscured)
- Использовать несколько поисков (поиск, уточнение, поиск)

### Скрипт не запускается
```bash
python -m pip install --upgrade pycryptodome
python test_network_hijacking.py
```

### Адреса не совпадают после перезагрузки
- Это нормально — ASLR перемешивает адреса
- Нужно переканировать на каждой новой сессии

### Изменения не применились в игре
- Убедиться, что адрес правильный
- Проверить, что процесс всё ещё запущен
- Попробовать Cheat Engine сначала для проверки

---

## 📞 Справка

**F-06 (компрометированный пароль):**
```
salt = IV = файл[0:16]
key = PBKDF2-HMAC-SHA1(пароль, salt, 100, 16)
cipher = AES-128-CBC + PKCS#7
```

**F-07 (структура сундука):**
```
+0x10  ItemId(защ.)
+0x20  Type(защ.)          ← 910651 обычный, 920651 редкий
+0x30  RewardItemId(защ.)  ← то, что выпадет
```

**F-08 (защита Obscured):**
```
формула: значение = ((шифротекст − ключ) ^ ключ)
```

---

## ✨ Удачи в тестировании!

Документы:
- `UNCHECKED_VECTORS.md` — полный анализ
- `TEST_PROTOCOL_UNCHECKED.md` — пошаговые протоколы  
- `QUICK_SUMMARY.txt` — визуальная сводка

Вопросы? Смотрите документацию или переанализируйте F-01...F-08 в FINDINGS.md
