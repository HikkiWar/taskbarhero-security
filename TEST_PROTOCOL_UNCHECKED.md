# TaskBarHero — Протокол тестирования непроверённых векторов

**Версия:** 1.0  
**Статус:** К исполнению  
**Приоритет:** Критичный  

---

## Критерий успеха

Все тесты следуют **протоколу F-07/F-08** из FINDINGS.md:
1. **Правка памяти** → дождаться **автосейва игры** (~3 мин)
2. **Полный рестарт** клиента
3. **Перечитать сейв** → расшифровать → проверить **значение пережило рестарт**
4. Если пережило → **пережило синхронизацию с сервером** → УЯЗВИМО

---

## Вектор 1: Network Hijacking (CRITICAL)

### Цель
Проверить, может ли сервер принять **полностью переш cifрованный сейв** с подменёнными данными.

### Предусловие
- Рабочий сейв (можно из test-results)
- Python 3 + `pycryptodome`
- x64dbg или Cheat Engine для извлечения пароля (из F-06 известна методика)

### Шаги

#### Этап 1: Извлечь пароль сейва (если неизвестен)
```bash
# Из phase1_background.txt и F-06 известно:
# Перехват Rfc2898DeriveBytes через VEH
# Результат: пароль в RDX, соль в R8
# Стандартная длина: 16 байт
```

**Действие:** Запустить x64dbg, перехватить конструктор → прочитать пароль  
**Ожидаемо:** Одна 16-байтовая строка (или похожего размера)  

#### Этап 2: Расшифровать сейв
```python
from Crypto.Protocol.KDF import PBKDF2
from Crypto.Cipher import AES
from Crypto.Util.Padding import unpad

# Прочитать файл
with open('save_file.sav', 'rb') as f:
    data = f.read()

salt = data[:16]  # IV = salt
encrypted = data[16:]

# Генерировать ключ
key = PBKDF2(password, salt, 16, 100, 'sha1')

# Расшифровать
cipher = AES.new(key, AES.MODE_CBC, salt)
decrypted = unpad(cipher.decrypt(encrypted), 16)

# Сохранить
with open('save_decrypted.bin', 'wb') as f:
    f.write(decrypted)
```

**Действие:** Запустить скрипт  
**Ожидаемо:** Файл `save_decrypted.bin` (~5-50 МБ)  

#### Этап 3: Модифицировать сейв
Найти и изменить **одно значение**, которое легко отследить:

```python
# Вариант A: Изменить maxCompletedStage
# Текущее значение из FINDINGS: 1207
# Изменить на: 1307 (заметный скачок)

# Вариант B: Изменить HeroExp
# Найти паттерн: 0x12 0x34 0x56 0x78 (int32) с защитой Obscured
# Использовать формулу из F-08

# Вариант C: Изменить количество золота
# Обычно в начале структуры валюты
```

**Действие:** Бинарный редактор → найти → изменить  
**Ожидаемо:** Один или несколько байт изменены, сумма хеша не совпадает  

#### Этап 4: Перешифровать
```python
from Crypto.Cipher import AES
from Crypto.Util.Padding import pad

# Прочитать модифицированный
with open('save_decrypted_modified.bin', 'rb') as f:
    modified = f.read()

# Переш cifровать тем же ключом и солью
padded = pad(modified, 16)
cipher = AES.new(key, AES.MODE_CBC, salt)
encrypted_new = cipher.encrypt(padded)

# Соберём новый файл
with open('save_hijacked.sav', 'wb') as f:
    f.write(salt)  # IV в начало
    f.write(encrypted_new)
```

**Действие:** Запустить скрипт  
**Ожидаемо:** Файл `save_hijacked.sav` того же размера  

#### Этап 5: Закинуть в папку сохранений
```bash
# Путь обычно:
# C:\Users\<User>\AppData\LocalLow\<Developer>\<Game>\
# или
# C:\Users\<User>\Documents\My Games\

# Заменить оригинальный файл копией hijacked
cp save_hijacked.sav <save_folder>/save_slot_1.sav
```

**Действие:** Скопировать файл  
**Ожидаемо:** Оригинал перезаписан  

#### Этап 6: Запустить игру
**Действие:** Запустить TaskBarHero, загрузить сейв  
**Возможные исходы:**
- ✅ **УЯЗВИМО:** Значение из этапа 3 **принялось**, видно в UI
- ❌ **ЗАЩИТА:** Сервер отправил оригинальное значение, откатил
- ⚠️ **НЕОПРЕДЕЛЁННО:** Ошибка загрузки, рекомендовать откат

#### Этап 7: Проверить после автосейва и рестарта
Если игра загрузилась:
1. Дождаться **3 минут** (автосейв)
2. **Полностью закрыть** игру
3. Расшифровать новый сейв (шаги 1-2)
4. Проверить, **осталось ли изменённое значение**

**Ожидаемо:** Если УЯЗВИМО → значение сохранилось на сервере

### Результат
Заполнить таблицу:

| Тест | Значение | Было | Стало | Пережило рестарт | Вывод |
|------|----------|------|-------|------------------|-------|
| maxCompletedStage | 1207 | 1307 | ? | ? | ? |
| HeroExp | ? | ? | +1M | ? | ? |
| Gold | ? | ? | +100k | ? | ? |

---

## Вектор 2: Cube Synthesis (HIGH)

### Цель
Проверить валидацию при синтезе кубов. Аналогично F-01 (отрицательная цена).

### Предусловие
- Игра запущена
- Герой с доступом к синтезу кубов
- Cheat Engine для поиска структуры

### Шаги

#### Этап 1: Найти CubeSynthesisLog в памяти
```
Сканировать по: CubeSynthesisLog или 0x0C 0x75 0x62 0x65
Или: найти "CubeSynthesis" в strings
```

**Действие:** Cheat Engine → String scanner  
**Ожидаемо:** Несколько адресов, один из них указывает на актуальную структуру  

#### Этап 2: Найти рецепт синтеза в памяти
```
CubeRecipeSaveData должна содержать:
- id рецепта
- требуемые ингредиенты (массив)
- стоимость (gold/materials)
- уровень синтеза
- награда (результат)

Поиск: известный ID рецепта (напр. 1001) как int32
```

**Действие:** Cheat Engine → Exact Value Scan  
**Ожидаемо:** Найден адрес, окружен другими полями структуры  

#### Этап 3: Найти стоимость синтеза
Обычно рядом с ID:
```
[ID рецепта] [уровень] [стоимость_1] [стоимость_2]
```

**Действие:** Проанализировать соседние значения  
**Ожидаемо:** Числа, которые соответствуют ингредиентам  

#### Этап 4: Изменить стоимость на отрицательное значение
Как в F-01:
```
Было: cost = 1000 (int32)
Стало: cost = -1000 (int32)
```

**Действие:** Чит-движок → изменить на отрицательное  
**Ожидаемо:** Числовое значение = -1000  

#### Этап 5: Попытаться синтезировать
**Действие:** В игре выбрать рецепт, начать синтез  
**Возможные исходы:**
- ✅ **УЯЗВИМО:** Синтез пошёл, ресурсы **добавились** вместо траты
- ❌ **ЗАЩИТА:** Ошибка "недостаточно материалов" или синтез отклонён
- ⚠️ **СЛОЖНО:** Требуется дополнительный анализ формул

#### Этап 6: Дождаться автосейва, перезагрузить
**Действие:** 3 минуты + перезагрузка + проверка расшифрованного сейва  
**Ожидаемо:** Если УЯЗВИМО → ресурсы остались на счету

### Результат

| Тест | Стоимость | Было | Изменено | Синтез прошёл | Сохранилось | Severity |
|------|-----------|------|----------|---------------|------------|----------|
| Отрицательная цена | -1000 | ? | ? | ? | ? | HIGH |
| Нулевой уровень | 0 → требуется 5 | ? | ? | ? | ? | HIGH |
| Чужой ID материала | ? → 999999 | ? | ? | ? | ? | MEDIUM |

---

## Вектор 3: Rare Box Rolls (HIGH)

### Цель
Проверить, может ли измениться рарность сундука в памяти и остаться после рестарта.

### Предусловие
- BoxData адреса известны из F-07 (`box_queue_prediction.json`)
- Надо сохранить адреса в новой сессии (ASLR)
- Cheat Engine или x64dbg

### Шаги

#### Этап 1: Перезагрузить игру, найти новые адреса BoxData
```python
# Из equip_chain.md известна база:
base = 0x7FFB38A60000  # может меняться!

# CubeInData, BoxData должны быть рядом
# Поиск: значение из box_queue_prediction.json (например, 12030)
```

**Действие:** Cheat Engine → Exact Value Scan → Value = 12030  
**Ожидаемо:** Найдены адреса, один из них для `ItemId`  

#### Этап 2: Выявить структуру BoxData
Из F-07 известна раскладка:
```
+0x10 ItemId(защ.)        // 12030
+0x20 UniqueKey(защ.)     // 910651 обычный или 920651 редкий
+0x28 ClaimableAt(защ.)
+0x30 RewardItemId(защ.)  // то, что упадёт
+0x40 RewardItemUniqueID(защ.)
+0x48 IsGet (обычный int)
```

**Действие:** Прочитать соседние смещения  
**Ожидаемо:** Структура совпадает  

#### Этап 3: Найти "редкий" сундук
Нужен сундук с типом 920651 (редкий) или с дорогой наградой (ID > 500000).

**Вариант A:** Открыть много сундуков → найти редкий → запомнить адрес  
**Вариант B:** Найти в box_queue_prediction.json сундук с типом 920651  

**Действие:** Поиск  
**Ожидаемо:** Найден адрес редкого сундука  

#### Этап 4: Найти сундук, который будет открыт следующим (обычный)
Из F-07 известно: сундуки выданы сервером, они в очереди.

**Действие:** Отследить последовательность в коде или по временным меткам  
**Ожидаемо:** Получен адрес "следующего" сундука (910651)  

#### Этап 5: Скопировать данные редкого в обычный
Опасно! Но это прямой тест:
```
Сундук_обычный[+0x10:+0x50] = Сундук_редкий[+0x10:+0x50]
```

Или безопаснее: изменить только тип:
```
Сундук_обычный[+0x20] = 920651  // обычный → редкий
```

**Действие:** Cheat Engine → скопировать значения или просто изменить тип  
**Ожидаемо:** Байты изменены  

#### Этап 6: Открыть сундук в игре
**Действие:** В UI нажать на сундук, открыть  
**Возможные исходы:**
- ✅ **УЯЗВИМО:** Выпал **редкий предмет**, хотя сундук был обычный
- ❌ **ЗАЩИТА:** Выпал **обычный**, сервер вернул правильную награду
- ⚠️ **НЕОДНОЗНАЧНО:** Награда в памяти другая, чем выпала

#### Этап 7: Дождаться автосейва и проверить сейв
**Действие:** 3 минуты + перезагрузка + расшифровка сейва + проверка `RewardItemId`  
**Ожидаемо:** Если УЯЗВИМО → редкая награда осталась в сейве  

### Результат

| Тест | Исходный тип | Изменено на | Выпало | Сохранилось | Severity |
|------|-------------|-------------|--------|-------------|----------|
| Подмена типа 910→920 | 910651 | 920651 | ? | ? | HIGH |
| Подмена RewardItemId | 110001 | 520014 | ? | ? | HIGH |
| Копирование структуры | обычный | редкий | ? | ? | CRITICAL |

---

## Вектор 4: Drop Probability (MEDIUM)

### Цель
Найти таблицу шансов выпадения и проверить, может ли быть изменена локально.

### Предусловие
- Поиск по «весам» (float 0.7, 0.25, 0.05)
- Или поиск по ID предметов и вероятностям
- Cheat Engine

### Шаги

#### Этап 1: Найти таблицу весов в памяти
```
Поиск: float значение 0.7 или 0.05 (редкие вероятности)
Или: массив ID с float коэффициентами
```

**Действие:** Cheat Engine → Scan Float Value  
**Ожидаемо:** Найдено несколько адресов с близкими значениями  

#### Этап 2: Проверить структуру
Если найдена, должна быть:
```
[ID_предмета_1: int] [вес_1: float] [ID_предмета_2: int] [вес_2: float] ...
или
[вес_1: float] [вес_2: float] [вес_3: float] + отдельно ID
```

**Действие:** Проанализировать соседние значения  
**Ожидаемо:** Логическая структура выявлена  

#### Этап 3: Изменить вес редкого предмета на максимум
```
Было: редкие = 0.05 (5%)
Стало: редкие = 1.0 (100%)
Было: обычные = 0.7
Стало: обычные = 0.0
```

**Действие:** Cheat Engine → изменить float значение  
**Ожидаемо:** Вес изменён  

#### Этап 4: Открыть много сундуков
**Действие:** Открыть 10-20 сундуков подряд  
**Возможные исходы:**
- ✅ **УЯЗВИМО:** Все награды **редкие**, несмотря на изменение
- ❌ **ЗАЩИТА:** Сервер отправляет правильные награды, локальный вес игнорируется
- ⚠️ **НЕОБХОДИМО БОЛЬШЕ ДАННЫХ:** 50%+ редких — нужна статистика

#### Этап 5: Проверить статистику
```
Ожидания: если таблица локальная и вес = 100% → ВСЕ редкие
Реальность: ? редких из ? открытых

Коэффициент нужен > 10 сундуков для надёжности
```

**Действие:** Подсчитать редкие  
**Ожидаемо:** Ясный результат  

#### Этап 6: Проверить после автосейва
**Действие:** Если нужны редкие в сейве → расшифровать и проверить  
**Ожидаемо:** Награды соответствуют тому, что выпало в игре  

### Результат

| Тест | Вес редких | Было | Изменено на | Выпало редких | Вывод |
|------|-----------|------|-------------|---------------|-------|
| Максимальный вес | 0.05 | ? | 1.0 | ? | ? |
| Нулевой вес обычных | 0.7 | ? | 0.0 | ? | ? |

---

## Временная шкала

```
День 1:
  - Network Hijacking (этап 1-3): 2 ч (извлечение пароля, расшифровка)
  
День 2:
  - Network Hijacking (этап 4-7): 3 ч (переш cifровка, тестирование)
  - Cube Synthesis (этап 1-4): 2 ч (поиск структуры)
  
День 3:
  - Cube Synthesis (этап 5-6): 2 ч (тестирование)
  - Rare Box Rolls (этап 1-3): 2 ч (поиск BoxData)
  
День 4:
  - Rare Box Rolls (этап 4-7): 2 ч (модификация, тестирование)
  - Drop Probability (все этапы): 2 ч

Итого: ~15-17 часов
```

---

## Отчётность

Заполнить итоговую таблицу в формате FINDINGS.md:

```markdown
| ID | Находка | Severity | Статус |
|----|---------|----------|--------|
| F-09 | Подмена переш cifрованного сейва | ? | ✅/❌ |
| F-10 | Синтез куба с отрицательной ценой | ? | ✅/❌ |
| F-11 | Подмена типа сундука (редкий/обычный) | ? | ✅/❌ |
| F-12 | Локальная таблица весов дропа | ? | ✅/❌ |
```

Где `✅` = уязвимо, `❌` = защищено
